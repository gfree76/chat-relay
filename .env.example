# chat-relay configuration (copy to .env for local dev; .env is gitignored)

# Address to listen on
ADDR=:8080

# --- Firebase Cloud Messaging (for the real sendPush, not yet implemented) ---
# FCM_PROJECT_ID=your-firebase-project-id
# GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/service-account.json
